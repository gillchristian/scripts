{
  "private": true,
  "scripts": {
    "format": "prettier --write '**/*.{md,js,json}'",
    "format:changed": "pretty-quick",
    "precommit": "pretty-quick --staged"
  },
  "devDependencies": {
    "husky": "^0.14.3",
    "prettier": "^1.13.5",
    "pretty-quick": "^1.6.0"
  }
}
